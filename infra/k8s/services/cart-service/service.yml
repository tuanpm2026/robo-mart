apiVersion: v1
kind: Service
metadata:
  name: cart-service
  namespace: robomart
spec:
  selector:
    app: cart-service
  ports:
  - port: 8082
    targetPort: 8082
    protocol: TCP
    name: http
  type: ClusterIP
