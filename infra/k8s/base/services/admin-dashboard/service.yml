apiVersion: v1
kind: Service
metadata:
  name: admin-dashboard
  namespace: robomart
spec:
  selector:
    app: admin-dashboard
  ports:
  - port: 80
    targetPort: 80
    nodePort: 30174
    protocol: TCP
    name: http
  type: NodePort
