# Patches the default ServiceAccount in the robomart namespace so every pod
# inherits ghcr.io pull credentials without per-deployment edits.
# kubectl apply merges imagePullSecrets into the existing default SA.
apiVersion: v1
kind: ServiceAccount
metadata:
  name: default
  namespace: robomart
imagePullSecrets:
  - name: ghcr-pull-secret
