{
  "name": "robo-mart-frontend",
  "version": "0.0.1",
  "private": true,
  "workspaces": [
    "shared",
    "customer-website",
    "admin-dashboard"
  ],
  "lint-staged": {
    "frontend/{customer-website,admin-dashboard}/src/**/*.{ts,vue}": [
      "prettier --write",
      "oxlint --fix"
    ]
  },
  "devDependencies": {
    "lint-staged": "^16.4.0",
    "simple-git-hooks": "^2.13.1"
  }
}
