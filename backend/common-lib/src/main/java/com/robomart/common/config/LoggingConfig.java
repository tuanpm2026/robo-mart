package com.robomart.common.config;

import org.springframework.context.annotation.Configuration;

@Configuration
public class LoggingConfig {
    // Logging is configured via logback-spring.xml
    // This class serves as the configuration entry point and
    // can be extended with programmatic logging configuration if needed.
}
