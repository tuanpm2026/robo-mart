spring:
  application:
    name: cart-service
  profiles:
    active: dev
  data:
    redis:
      host: localhost
      port: 6379

server:
  port: 8082

robomart:
  cart:
    ttl-minutes: 1440

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics

---
spring:
  config:
    activate:
      on-profile: test
  data:
    redis:
      host: localhost
      port: 6379
